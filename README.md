# unitrans-data-pipeline
This project is an end-to-end pipeline that ingests live bus location data via API, stores it in a PostgreSQL database, and runs scheduled transformations using Apache Airflow. The final dataset is used for delay analytics per route.
